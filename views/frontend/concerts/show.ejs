<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Concert Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
  <div class="bg-white p-8 rounded-lg shadow-lg max-w-lg w-full">
    <h1 class="text-2xl font-bold mb-6 text-center">Concert Details</h1>
    <div class="mb-6">
      <% if (concert) { %>
        <!-- Assurez-vous que la variable concert est correctement définie -->
        <img src="<%= concert.picture ? '/uploads/' + concert.picture : '/path/to/default/image.jpg' %>" alt="<%= concert.name %>" class="w-full h-48 object-cover rounded-lg mb-4">
        <h2 class="text-xl font-semibold mb-2"><%= concert.name %></h2>
        <p class="text-gray-700 mb-2"><strong>Date:</strong> <%= new Date(concert.date).toLocaleDateString() %></p>
        <p class="text-gray-700 mb-2"><strong>Location:</strong> <%= concert.location %></p>
        <p class="text-gray-700 mb-2"><strong>Genre:</strong> <%= concert.genre %></p>
        <p class="text-gray-700 mb-2"><strong>Description:</strong></p>
        <p class="text-gray-700 mb-2"><%= concert.description %></p>
        <p class="text-gray-700 mb-4"><strong>Price:</strong> $<%= concert.price %></p>
      <% } else { %>
        <p class="text-red-500">Concert not found.</p>
      <% } %>
    </div>
    <div class="flex justify-between">
      <% if (concert) { %>
        <a href="/concerts/edit/<%= concert._id %>" class="bg-yellow-500 text-white px-4 py-2 rounded-lg hover:bg-yellow-600 transition duration-200">Edit</a>
        <form action="/concerts/<%= concert._id %>?_method=DELETE" method="POST">
          <button type="submit" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition duration-200">Delete</button>
        </form>
      <% } %>
    </div>
    <div class="mt-4 text-center">
      <a href="/concerts" class="text-blue-500 hover:underline">Back to Concerts List</a>
    </div>
  </div>
</body>
</html>
